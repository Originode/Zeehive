version: 1
label: Zee Base
glyph: "🐝"
summary: The foundation every zee stands on — the cxell-zee manual, fully incorporated.

# The full cxell-zee manual is incorporated as this harness's memory. loadHarnessDir resolves a
# memory path against the harness folder first, then the repo root — so this reads the LIVE manual
# (no copy, no drift). Every harness that inherits Zee Base carries the manual with it.
memory:
  - docs/cxell-zee-manual.md
