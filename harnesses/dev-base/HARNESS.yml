version: 1
label: Dev Base
summary: >
  The shared craft layer for the dev crew — orienting in an unknown repo, working in small verified
  steps, and reporting honestly. Project-agnostic: it names no repo, no path and no container.
glyph: "⌁"
zee_type: worker

# THE ONE PLACE SHARED TEXT LIVES. Eight role harnesses inherit this layer, so every line here is
# paid for eight times over — and every line duplicated into a role is paid for twice. Keep the
# craft here and keep the roles to the judgement that is theirs alone (docs/dev-crew.md).
#
# It inherits Zee Base, which carries the cxell-zee manual: the manual arrives ONCE, by inheritance,
# and is never restated in this subtree. A role that re-explains a verb has taught the wearer a
# second, staler copy of its own law.
parent: zee-base

memory:
  - memory/dev-loop.md

skills:
  - orient-in-a-new-repo
