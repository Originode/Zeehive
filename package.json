{
  "name": "zeehive",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic agent-environment orchestrator: xells (isolated envs), zees (agents), queenzee (pure-script orchestrator).",
  "workspaces": [
    "server",
    "web",
    "mcp"
  ],
  "scripts": {
    "db:migrate": "node server/src/db/migrate.js",
    "db:seed": "node server/src/db/seed.js",
    "db:reset": "npm run db:migrate && npm run db:seed",
    "server": "node server/src/index.js",
    "queenzee": "node server/src/queenzee/index.js",
    "web": "npm --workspace web run dev",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run server\" \"npm run web\""
  },
  "author": "Mark Revelo",
  "license": "UNLICENSED"
}
