---
name: ooney
description: Ship this xell's work to PRODUCTION through the gate cascade — sync with main, schema parity with prod, clean builds, human clearance, queenzee-built deploy. Use when the user types "/ooney" (optionally "server", "webapp", or "both"), or when you believe your work is ready to go live.
allowed-tools: Bash
---

The user invoked `/ooney`. This is the current verdict of the ship gate for this session's xell,
measured live by the queenzee:

!`node "${ZEEHIVE_HOME:-D:/Repos/Zeehive}/scripts/xell-ooney.mjs" $ARGUMENTS`

**The output above IS the procedure.** It is generated by the queenzee from live state — do not
improvise around it, summarize it away, or substitute steps from memory. Do exactly what the
failing gate says, then re-run.

To be told when the verdict changes instead of polling by hand, run in the **background**:

```
node "${ZEEHIVE_HOME:-D:/Repos/Zeehive}/scripts/xell-ooney.mjs" $ARGUMENTS --wait
```

Its exit is your nudge. Read the **final output** for the verdict — `live` means your work is in
production; `deny` states exactly what to fix. (Exit 0 either way: a deny is the gate answering,
not the command failing. Non-zero means the check itself could not run.)
